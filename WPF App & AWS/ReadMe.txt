This is a WPF app with the following functionalities: 
1. Create DynamoDB tables to store book(s) on user’s bookshelf and store reading snapshot
2. Add book to bookshelf
3. Add reading snapshot to DynamoDB
4. List all books in specified user’s bookshelf
5. Search/display particular user’s most recent reading snapshot 
